image: node:latest

cache:
  paths:
    - node_modules

stages:
  - publish

pages:
  stage: publish
  script:
    - yarn
    - yarn export
    - rm -rf public
    - mv out public
  artifacts:
    paths:
      - public
  only:
    - main
